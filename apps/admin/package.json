{
  "name": "@apps/admin",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 3100",
    "build": "tsc && vite build",
    "lint": "eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0",
    "preview": "vite preview",
    "storybook": "storybook dev -p 6006",
    "storybook:build": "storybook build -o build-storybook-static",
    "clean:build": "rm -rf dist/ .turbo/ node_modules/ build-storybook-static/"
  },
  "dependencies": {
    "@configs/storybook": "*",
    "@core/providers": "*",
    "@styles/ui-mui": "*",
    "@utils/version": "*"
  },
  "devDependencies": {}
}

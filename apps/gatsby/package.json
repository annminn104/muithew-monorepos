{
  "name": "@apps/gatsby",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "gatsby develop -p 3600",
    "start": "gatsby serve",
    "build": "gatsby build",
    "clean": "gatsby clean",
    "typecheck": "tsc --noEmit",
    "clean:build": "rm -rf .turbo/ .cache/ public/ node_modules/ storybook-static/"
  },
  "dependencies": {
    "gatsby": "^5.13.3"
  },
  "devDependencies": {}
}

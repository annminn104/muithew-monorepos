import { Grid } from '@mui/material';

const SectionServices = () => {
  return (
    <Grid container>
      <Grid item xs={12}>
        SectionServices
      </Grid>
    </Grid>
  );
};

export default SectionServices;

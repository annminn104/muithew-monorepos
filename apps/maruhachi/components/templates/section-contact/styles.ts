'use client';

import { Typography, styled } from '@mui/material';

export const SecContactTitle = styled(Typography, {})(({ theme }) => ({ textAlign: 'center', color: theme.palette.maruhachi['contrastText'] }));

'use client';

import { Button, styled } from '@mui/material';

export const ContactFormWrap = styled('form', {})(({}) => ({}));

export const ContactFormSubmit = styled(Button, {})(({}) => ({ height: '60px', fontSize: '24px', fontWeight: 'bold' }));

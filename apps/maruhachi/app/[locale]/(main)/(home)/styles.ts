'use client';

import { Container, styled } from '@mui/material';

export const HomePageWrap = styled(Container, {})(({ theme }) => ({}));

export const HomePageSection = styled('section', {})(({ theme }) => ({ padding: '60px 0' }));

{
  "name": "@apps/landing",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev -p 3200",
    "build": "next build",
    "start": "next start",
    "lint": "eslint . --max-warnings 0",
    "storybook": "storybook dev -p 9009",
    "clean:build": "rm -rf dist/ .turbo/ node_modules/ storybook-static/",
    "storybook:build": "storybook build --quiet -o storybook-static",
    "storybook:move": "mv storybook-static ../../storybook-static/landing"
  },
  "dependencies": {
    "@core/providers": "*",
    "@styles/ui": "*",
    "@configs/eslint": "*",
    "@configs/typescript": "*"
  }
}

'use client';

import { styled } from '@mui/material';

export const SectionSkillsWrap = styled('div', {})(({ theme }) => ({}));

export const SectionSkillsTitle = styled('h3', {})(({ theme }) => ({ fontSize: '40px', textAlign: 'center' }));

export const SectionSkillsSubtitle = styled('div', {})(({ theme }) => ({}));

export const SectionSkillsItem = styled('div', {})(({ theme }) => ({}));
